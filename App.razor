@*
    The Router component displays whichever component has a @page
    directive matching the current URI.
*@
@*FallbackComponent permet de rediriger les erreurs d'url vers la page spécifiée*@
<Router AppAssembly="typeof(Startup).Assembly" 
        FallbackComponent="typeof(BlazorTests.Pages.Errors.Error404View)"/>
