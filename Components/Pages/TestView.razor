@page "/test"

<LongRunningProcessButton ButtonText="Rechercher" Action="@Search"></LongRunningProcessButton>

@functions {
    private async Task Search()
    {
        await Task.Delay(1000);
    }
}